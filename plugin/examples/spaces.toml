# Sample ~/.wenlan/spaces.toml — copy this file to ~/.wenlan/spaces.toml
# and edit to taste.
#
# Maps a cwd path-prefix to a space name. Longest matching prefix wins.
# `~` is expanded to your home directory.
#
# Tiebreak: if two mappings have identical-length prefixes both matching
# the cwd, the first-defined mapping wins.

[[mapping]]
prefix = "~/Repos/wenlan"
space  = "wenlan"

[[mapping]]
prefix = "~/notes/brainstorms"
space  = "ideas"

[[mapping]]
prefix = "~/notes/career"
space  = "career"

[[mapping]]
prefix = "~/notes/health"
space  = "health"
