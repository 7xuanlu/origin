[package]
name = "wenlan-mcp"
version.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true
description = "MCP server for Wenlan, the local-first personal agent memory layer"
homepage = "https://wenlan.app"
documentation = "https://docs.rs/wenlan-mcp"
readme = "README.md"
keywords = ["memory", "mcp", "agent", "claude", "wenlan"]
categories = ["development-tools", "command-line-utilities"]

[lib]
name = "wenlan_mcp"
path = "src/lib.rs"

[[bin]]
name = "wenlan-mcp"
path = "src/main.rs"

[dependencies]
rmcp = { version = "~1.5", features = ["server", "transport-io", "transport-streamable-http-server"] }
# rmcp 1.x only requires `rmcp-macros = "^X.Y.0"`, so an unlocked `cargo install`
# resolves the newest macros crate, whose `#[tool]` expansion calls helpers the
# pinned rmcp does not have (`schema_for_input`, first-run gauntlet finding F3).
# Keep the macros crate on the same minor as rmcp.
rmcp-macros = "~1.5"
reqwest = { version = "0.12", default-features = false, features = ["json", "rustls-tls"] }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
schemars = "1"
tokio = { version = "1", features = ["full"] }
clap = { version = "4", features = ["derive"] }
tracing = "0.1"
tracing-subscriber = { version = "0.3", features = ["env-filter"] }
anyhow = "1"
thiserror = "2"
axum = "0.8"
tower-http = { version = "0.6", features = ["cors"] }
rand = "0.10"
base64 = "0.22"
subtle = "2"
http = "1"
dirs = "6"
semver = "1"
wenlan-types = { workspace = true }

[dev-dependencies]
tempfile = "3"
portpicker = "0.1"
wiremock = "0.6"
wenlan-server = { path = "../wenlan-server" }
wenlan-core = { path = "../wenlan-core" }
